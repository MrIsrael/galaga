import React from 'react'
import ReactDOM from 'react-dom'
import App from './App'

ReactDOM.render(<App />, document.getElementById('root'))

// document.getElementById('root').addEventListener('keydown', function(){ console.log(KeyboardEvent.keyCode) })